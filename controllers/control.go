package controllers

import(
	// "context"
	// "database/sql"
	// "fmt"
	// "time"
	// _ "github.com/go-sql-driver/mysql"
	// database "logsquery/database"
	)

func UpdateHttpHost(){
	//Khởi tạo biến conection
	// db := database.DBcon() 
	// truncate   := "TRUNCATE domain"
	// queryinsrt := "INSERT INTO domain(http_host) WHERE (SELECT distinct(http_host) FROM access_  )"
}

func CountResult(search []string,table string){
	//Khởi tạo biến conection
	// db := database.DBcon() 
	// queryinsrt := "SELECT * FROM "+table+" WHERE %s"
}